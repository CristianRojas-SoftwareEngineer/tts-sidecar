version: 2.1

orbs:
  python: circleci/python@2.1.1
  windows: circleci/windows@5.0

jobs:
  test:
    docker:
      - image: cimg/python:3.13
    steps:
      - checkout
      - run:
          name: Install system dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y libasound2-dev
      - python/install-packages:
          package-manager: pip
          pip-dependency-file: requirements.txt
      - run:
          name: Install pytest
          command: pip install pytest==9.1.1
      - run:
          # WARNING-04: CLAUDE.md documenta py_compile como chequeo de verificación,
          # pero no corría en CI; compileall detecta errores de sintaxis en módulos
          # sin tests propios o importados condicionalmente, antes del build.
          name: Check syntax (py_compile)
          command: python -m compileall src/
      - run:
          name: Run tests
          command: pytest tests/ -v

  build-windows:
    executor: win/server-2022
    steps:
      - checkout
      - run:
          # SUGGESTION-06: sin pin, un release nuevo de Chocolatey puede romper
          # el build sin cambios en el repo. Versión fijada al momento de escribir
          # este pin; actualizar deliberadamente cuando se quiera adoptar una nueva.
          name: Install Python 3.13
          command: choco install python313 -y --version=3.13.14
      - run:
          name: Install dependencies
          command: |
            python --version
            python -m pip install --upgrade pip
            python -m pip install -r requirements.txt
            python -m pip install pyinstaller==6.21.0
            choco install innosetup -y --version=6.3.3
      - run:
          name: Build Windows
          command: python scripts/build_windows.py
      - run:
          # store_artifacts no expande globs: se copia el instalador (cuyo nombre
          # incluye la versión) a un nombre estable, independiente de __version__.
          name: Stage installer artifact
          command: Copy-Item dist/tts-sidecar-*-setup.exe dist/tts-sidecar-setup.exe
      - store_artifacts:
          path: dist/tts-sidecar-setup.exe
          destination: tts-sidecar-win32-x86_64-setup.exe
      - store_artifacts:
          path: dist/tts-sidecar
          destination: tts-sidecar-win32-x86_64/

  build-linux-x64:
    docker:
      - image: cimg/python:3.13
    steps:
      - checkout
      - run:
          name: Install system dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y patchelf libasound2-dev
      - python/install-packages:
          package-manager: pip
          pip-dependency-file: requirements.txt
      - run:
          name: Install PyInstaller
          command: |
            pip install pyinstaller==6.21.0
      - run:
          name: Build Linux x64
          command: python scripts/build_linux.py --arch x86_64
      - run:
          # store_artifacts no expande globs: se copia el AppImage (cuyo nombre
          # incluye la versión) a un nombre estable, independiente de __version__.
          name: Stage AppImage artifact
          command: cp dist/tts-sidecar-*-x86_64.AppImage dist/tts-sidecar-x86_64.AppImage
      - store_artifacts:
          path: dist/tts-sidecar-x86_64.AppImage
          destination: tts-sidecar-linux-x86_64.AppImage
      - store_artifacts:
          path: dist/tts-sidecar
          destination: tts-sidecar-linux-x86_64/

  build-linux-arm64:
    machine:
      image: ubuntu-2204:current
    resource_class: arm.medium
    steps:
      - checkout
      - run:
          name: Install system dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y patchelf libasound2-dev
      - run:
          name: Set up Python 3.13
          command: |
            pyenv install -s 3.13
            pyenv global 3.13
            python --version
      - run:
          name: Install dependencies
          command: |
            python -m pip install --upgrade pip
            python -m pip install -r requirements.txt
            python -m pip install pyinstaller==6.21.0
      - run:
          name: Build Linux ARM64
          command: python scripts/build_linux.py --arch arm64
      - run:
          # store_artifacts no expande globs: se copia el AppImage (cuyo nombre
          # incluye la versión) a un nombre estable, independiente de __version__.
          name: Stage AppImage artifact
          command: cp dist/tts-sidecar-*-aarch64.AppImage dist/tts-sidecar-aarch64.AppImage
      - store_artifacts:
          path: dist/tts-sidecar-aarch64.AppImage
          destination: tts-sidecar-linux-aarch64.AppImage
      - store_artifacts:
          path: dist/tts-sidecar
          destination: tts-sidecar-linux-aarch64/

  build-darwin-universal2:
    macos:
      xcode: "26.4.0"
    resource_class: m4pro.medium
    steps:
      - checkout
      - run:
          name: Set up Python
          command: |
            python3 --version
            python3 -m pip install --upgrade pip
            pip3 install -r requirements.txt
      - run:
          name: Install PyInstaller
          command: |
            pip3 install pyinstaller==6.21.0
      - run:
          # create-dmg es un script de shell distribuido por Homebrew, no un
          # paquete de PyPI: `pip3 install create-dmg` fallaba siempre
          # (con o sin pin), independientemente de esta auditoría. Homebrew
          # no soporta fijar una versión exacta de forma simple (a diferencia
          # de pip), así que se deja sin pin explícito.
          name: Install create-dmg (Homebrew)
          command: |
            brew install create-dmg
      - run:
          name: Build macOS universal2
          command: python3 scripts/build_macos.py --arch universal2
      - run:
          # store_artifacts no expande globs: se copia el .dmg (cuyo nombre
          # incluye la versión) a un nombre estable, independiente de __version__.
          name: Stage DMG artifact
          command: cp dist/tts-sidecar-*-universal2.dmg dist/tts-sidecar-universal2.dmg
      - store_artifacts:
          path: dist/tts-sidecar-universal2.dmg
          destination: tts-sidecar-darwin-universal2.dmg
      - store_artifacts:
          path: dist/tts-sidecar-universal2.app
          destination: tts-sidecar-darwin-universal2.app/

workflows:
  build-all:
    jobs:
      - test
      - build-windows:
          requires:
            - test
      - build-linux-x64:
          requires:
            - test
      - build-linux-arm64:
          requires:
            - test
      - build-darwin-universal2:
          requires:
            - test
