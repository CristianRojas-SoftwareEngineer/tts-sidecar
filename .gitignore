node_modules/
build/
dist/
dist_test/
# Voces de usuario ignoradas, pero la voz de fábrica 'default' se versiona
# (se empaqueta vía --add-data y sirve de voz por defecto).
voices/*
!voices/default/
# Artefactos de build del AppImage
AppDir/
tts-sidecar.png
*.spec
# Config de Claude Code: ignorar todo .claude/ (skills, sesión, etc.)
.claude/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
# Artefactos de coverage: generados por `pytest --cov`, no se versionan.
.coverage
coverage.json
coverage.xml

# Lockfile de uv (proyecto usa requirements-lock.txt)
uv.lock
